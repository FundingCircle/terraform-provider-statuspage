pipeline:
  clone:
    image: plugins/git
    tags: true

  test:
    image: golang:1.12
    commands:
      - go test ./... -race

  release:
    image: golang:1.12
    secrets: [github_token]
    commands:
      curl -sL https://git.io/goreleaser | bash
    when:
      event: tag
  
